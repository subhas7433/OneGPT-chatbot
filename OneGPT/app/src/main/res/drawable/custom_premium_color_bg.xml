<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <!-- Define the button's background when pressed -->
            <solid android:color="@color/premium_clr" /> <!-- Replace with your desired color -->
            <corners android:radius="@dimen/msg_radious" /> <!-- Adjust the radius as per your preference -->
        </shape>
    </item>
    <item>
        <shape>
            <!-- Define the button's default background -->
            <solid android:color="@color/premium_clr" /> <!-- Replace with your desired color -->
            <corners android:radius="@dimen/msg_radious" /> <!-- Adjust the radius as per your preference -->
        </shape>
    </item>
</selector>